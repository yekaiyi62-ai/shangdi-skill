# 功能Skill SKILL.md 模板

Phase 3 构建时，读取此模板填充内容。

---

```markdown
---
name: [skill-name]
description: |
  [一句话功能描述]。基于[领域知识来源]的深度调研，
  提供[核心能力1]、[核心能力2]、[核心能力3]等功能。
  当用户提到「[触发词1]」「[触发词2]」「[触发词3]」时使用。
  即使用户只是说「[模糊触发1]」「[模糊触发2]」也应触发。
---

# [Skill名称]

> [一句话定位语]

## 能力范围

**能做的**：
- [核心能力1]
- [核心能力2]
- [核心能力3]

**不能做的**：
- [明确的边界1]
- [明确的边界2]

---

## 问题路由

收到用户输入后，先判断类型，走不同处理流程：

| 用户输入类型 | 场景 | 需要的工具 |
|------------|------|-----------|
| [输入类型1] | → 场景A | [工具列表] |
| [输入类型2] | → 场景B | [工具列表] |
| [输入类型3] | → 场景C | [工具列表] |
| 不确定/模糊 | → 引导用户澄清 | 无 |

---

## 执行工作流

### 场景A: [场景名称]

```
Step 1: [步骤名称]
  → [具体操作描述]
  → 输入：[这一步需要什么]
  → 输出：[这一步产出什么]

Step 2: [步骤名称]
  → [具体操作描述]
  → 【检查点】[展示什么给用户确认]

Step 3: [步骤名称]
  → [具体操作描述]
  → 输出格式：[按输出格式规范输出]
```

### 场景B: [场景名称]
```
[同上结构]
```

### 场景C: [场景名称]
```
[同上结构]
```

---

## 知识读取规则

> 当此Skill作为团队成员时，必须填写此节。独立Skill可省略。

### 启动时必读

1. `shared/user-profile.md` — 了解用户当前水平和目标
2. `shared/progress.md` — 了解最近练习情况
3. `members/[self]/references/[核心知识文件]` — 加载专属知识

### 按场景读取

| 场景 | 读取文件 |
|------|---------|
| [场景A] | `members/[self]/references/[xxx].md` + `references/official-standards.md` |
| [场景B] | `members/[self]/references/[xxx].md` + `shared/progress.md` |
| [场景C] | `references/domain-overview.md` + `shared/user-profile.md` |

### 完成后写入

1. 将本次结果追加到 `shared/progress.md`
2. 如果发现水平显著变化 → 更新 `shared/user-profile.md`
3. 如果发现新弱点 → 追加到 `shared/weak-points.md`

---

## 领域知识

> 独立Skill：直接在此处填写领域知识。
> 团队成员Skill：核心知识放在 `references/` 目录下，此处放简要摘要和引用。

### [知识模块1]

[从Phase 2调研中填充的专业知识。
包括：评分标准、方法论、行业规范、最佳实践等。
这里的内容必须是可操作的规则，不是泛泛的概述。
团队成员模式下，详细知识存放在 `members/[self]/references/` 目录。]

### [知识模块2]

[同上]

---

## 输出格式规范

### [输出类型1] 的格式

```
[具体的输出模板/结构]
```

### [输出类型2] 的格式

```
[具体的输出模板/结构]
```

---

## 质量检查

每次输出前，自动对照以下清单：

- [ ] 输出符合格式规范？
- [ ] 使用了专业术语和行业标准？
- [ ] 给出了具体可操作的建议，而非泛泛而谈？
- [ ] 标注了不确定的部分？
- [ ] 输出长度适中，信息密度高？

---

## 诚实边界

此Skill的已知局限：
- [具体局限1]
- [具体局限2]
- [具体局限3]
- 创建时间：[日期]

---

## 附录：调研来源

调研过程详见 `references/research/` 目录。

### 主要来源
- [来源1]
- [来源2]
```
